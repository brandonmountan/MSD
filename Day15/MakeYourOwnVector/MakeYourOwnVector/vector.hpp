//
//  vector.hpp
//  MakeYourOwnVector
//
//  Created by Brandon Mountan on 9/10/24.
//

#ifndef vector_hpp
#define vector_hpp

#include <stdio.h>

#endif /* vector_hpp */


struct myVector {
    int capacity;
    int size;
    int* arrayPtr;
//    makeVector(initialCapacity);
//    freeVector(MyVec);
//    pushBack(myVector, 5);
//    popBack(myVector, 5);
//    get(myVec, index);
//    set(myVec, index, newValue);
//    growVector(myVec);
};
